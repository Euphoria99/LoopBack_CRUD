pipeline {
    agent any

    stages {
        stage('Build') {
            steps{
              echo 'building works'
            }
        }
        stage('test') {
            steps{
              echo 'testing works'
            }
        }
    }
}
